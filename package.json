{
  "name": "autoshim-monorepo",
  "private": true,
  "license": "Apache-2.0",
  "engines": { "node": ">=20" },
  "scripts": { "test": "vitest run", "build": "pnpm -r build" },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3.0.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
