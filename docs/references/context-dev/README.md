# Context.dev documentation mirror

Mirrored 2026-09-01 from docs.context.dev (their published llms.txt / llms-full.txt).

- `llms.txt` — index of every docs page (each URL also serves raw markdown with a `.md` suffix)
- `llms-full.txt` — the COMPLETE documentation in one file (~1MB). Search this before writing any code that touches Context.dev.
- The agent skill lives at `.claude/skills/context-dev/SKILL.md` (from docs.context.dev/skill.md).

Rule: never write Context.dev API code from memory — grep this mirror (or re-fetch the live page) for the exact endpoint, params, and response shape first. Refresh the mirror when their changelog moves: https://docs.context.dev/changelog
